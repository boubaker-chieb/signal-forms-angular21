import { Routes } from '@angular/router';
import { SignIn } from './01-signal-forms/sign-in/sign-in';

export const routes: Routes = [
    {
        path: "signal-forms",
        component: SignIn
    }
];
